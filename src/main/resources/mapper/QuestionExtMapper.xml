<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.tit.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="cn.edu.tit.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Dec 26 20:18:49 GMT+08:00 2019.
        -->
        <id column="ID" jdbcType="INTEGER" property="id" />
        <result column="TITLE" jdbcType="VARCHAR" property="title" />
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
        <result column="GMT_MODIFY" jdbcType="BIGINT" property="gmtModify" />
        <result column="CREATOR" jdbcType="INTEGER" property="creator" />
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
        <result column="TAG" jdbcType="VARCHAR" property="tag" />
    </resultMap>

    <!-- 实现问题阅读数的增加 -->
    <update id="incViewCount">
        update QUESTION
        set  VIEW_COUNT = VIEW_COUNT + #{step,jdbcType=INTEGER}
        where id=#{id}
    </update>

    <!-- 实现评论的回复数的增加 -->
    <update id="incCommentCount">
        update QUESTION
        set  COMMENT_COUNT = COMMENT_COUNT + #{step,jdbcType=INTEGER}
        where id=#{id}
    </update>

    <!--使用正则表达式查找相似问题-->
    <select id="selectSimilarQuestion" parameterType="cn.edu.tit.community.model.Question" resultMap="BaseResultMap">
        select * from QUESTION
        <where>
            <if test="id != null">
                and id != #{id}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
        </where>
        order by gmt_create desc limit 20
    </select>
</mapper>